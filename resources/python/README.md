# Embedded Python Runtime

This folder is populated by `npm run bridge:python` before packaging the Windows desktop app.

The generated app installer includes this runtime under `resources/python`, so users do not need to install Python manually for the Zoom/OBS bridge.
